const initFilterItems = {
  install: function (vue, opts){
    // FilterItems.path.abrisham.selected = true;
    // FilterItems.path.oghianous.selected = false;
  }
};

export default initFilterItems
